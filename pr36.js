"use strict";
const make_shirt = (size, message) => {
    console.log(`size:'${size}', message:'${message}'`);
    `size:'${size}', nessage:'${message}'`;
};
let size = 'medium';
let message = ' its how its done';
make_shirt(size, message);
make_shirt("large", "nah its the wrong way");
